# Written by the running game, not by us
mod/BanList.json
mod/Players.json
mod/Settings.json
mod/Snapshots/*.json

__pycache__/
*.pyc

# local Claude Code tooling, not part of the mod
.claude/
mod/Bench.json
mod/Event.json
mod/Plots.json

# Somebody else's video transcript, fetched for research. Not ours to publish.
youtube/

# Game-written state that appears in the installed copy, not the repo
mod/Checklist.json
mod/Bridge.json
mod/Cmd-*.json
mod/Out-*.json

# Steam gallery images, rebuilt from Steam screenshots by dev/steam_images.py.
# picked.json is the CHOICE and is worth keeping -- the pngs are output.
dev/steam/*.png
