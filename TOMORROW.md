# Next session

Everything moved to **[docs/NEXT.md](docs/NEXT.md)** — one handover instead of
two drifting copies of the same list.

    python dev/check_all.py --sync      # then RESTART Scrap Mechanic

**Three things, in order.** Look at the middle of the city and say whether the
plaza is there; re-test `/lockdown` after a world reload; then `/bridge on` and
the rest moves to my side.

    python dev/checklist_report.py      # what is answered, and what failed
    python dev/bridge.py --status       # is the bridge listening

`docs/NEXT.md` is the full handover. Still to build: save presets, and a bot
you can hand over while you play.
